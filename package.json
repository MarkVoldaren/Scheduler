{
  "name": "scheduler-operations",
  "version": "1.0.0",
  "description": "Server-backed Scheduler Operations app with persistent CSV uploads and shared capacity settings.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check scheduler-core.js && node --check projects-domain.js && node --check projects-store.js && node --check projects-csv.js && node --check projects-ui.js && node --check projects-print.js",
    "test": "node --test tests"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "cookie-session": "^2.1.0",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  },
  "engines": {
    "node": ">=20"
  }
}
